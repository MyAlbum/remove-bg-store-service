[project]
name = "remove-bg-store-service"
version = "0.1.0"
description = "Standalone service for /api/studio/remove-bg-store compatibility"
requires-python = ">=3.11"

[tool.uvicorn]
factory = false
